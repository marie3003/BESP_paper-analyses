executor: slurm

default-resources:
  - mem_mb=4000
  - cpus_per_task=1
  - runtime=60

jobs: 500
latency-wait: 30
rerun-incomplete: true

slurm-logdir: logs
slurm-array-jobs: "simulate_alignment,run_beast,combine_runs"
slurm-array-limit: 1000